include ':FtcRobotController'
include ':TeamCode'

dependencyResolutionManagement {
    repositories {
        mavenLocal()    // 👈 REQUIRED for using LOCAL Pedro builds
        google()
        mavenCentral()
        maven { url = "https://jitpack.io" }
    }
}
